Options -Indexes<IfModule mod_rewrite.c>RewriteEngine On# /pagina -> /pagina.html (somente se existir)RewriteCond %{REQUEST_FILENAME} !-fRewriteCond %{REQUEST_FILENAME} !-dRewriteCond %{REQUEST_FILENAME}.html -fRewriteRule ^(.+?)/?$ $1.html [L]</IfModule>